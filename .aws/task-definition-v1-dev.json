{
  "family": "fastapi-bootstrap-v1-dev",
  "networkMode": "awsvpc",
  "requiresCompatibilities": ["FARGATE"],
  "cpu": "256",
  "memory": "512",
  "executionRoleArn": "arn:aws:iam::ACCOUNT_ID:role/fastapi-bootstrap-dev-task-execution-role",
  "taskRoleArn": "arn:aws:iam::ACCOUNT_ID:role/fastapi-bootstrap-dev-task-role",
  "containerDefinitions": [
    {
      "name": "fastapi-bootstrap-v1",
      "image": "ACCOUNT_ID.dkr.ecr.us-east-1.amazonaws.com/fastapi-bootstrap:latest",
      "essential": true,
      "portMappings": [
        {
          "containerPort": 8000,
          "hostPort": 8000,
          "protocol": "tcp"
        }
      ],
      "environment": [
        {
          "name": "ENVIRONMENT",
          "value": "dev"
        },
        {
          "name": "DEPLOYMENT_ID",
          "value": "DEPLOYMENT_ID_PLACEHOLDER"
        }
      ],
      "logConfiguration": {
        "logDriver": "awslogs",
        "options": {
          "awslogs-group": "/ecs/fastapi-bootstrap-dev",
          "awslogs-region": "us-east-1",
          "awslogs-stream-prefix": "ecs-v1"
        }
      },
      "healthCheck": {
        "command": ["CMD-SHELL", "curl -f http://localhost:8000/ready || exit 1"],
        "interval": 15,
        "timeout": 5,
        "retries": 3,
        "startPeriod": 60
      }
    }
  ]
}
